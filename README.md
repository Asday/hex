# Developing

Clone the repo, then:

```shell
pip install -r requirements-dev.txt
./watch-flake8
# In a separate shell
./watch-tests
# And another separate shell
./watch-protobuf
```

# Running tests

`./tests.py`.  Simple.
